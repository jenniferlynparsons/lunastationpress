---
import ImageLoader from './ImageLoader.astro';

const { imagePath, altText, className } = Astro.props;
---

<ImageLoader 
  folder="cover-images"
  imagePath={imagePath}
  altText={altText}
  className={`!m-0 w-auto ${className || ''}`}
  errorMode="warn"
/>
